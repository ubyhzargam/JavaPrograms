This code takes an array input from the user and sorts the array in ascending order using QuickSort technique and the partitioning of the array is done using Lomuto's partition. The main function then prints the final sorted array. The code to this can be accessed from .java file in the folder
