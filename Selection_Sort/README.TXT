This code takes the number of elements and the array elements as input from the user in the main function. The main function then calls SelectionSort function and passes the array as parameter to that function. This function implements the technique of Selection Sort through iteration and sorts the array and then returns the control to main function which then prints the sorted array. The code to this can be accessed from .java file
