This code takes a linked list as input from the user, it also takes the element to be inserted at the beginning of the linked list and modifies the linked list accordingly. The code then prints the modified linked list after inserting the element at the beginning. The code to this can be accessed from .java file.
