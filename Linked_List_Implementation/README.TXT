This code implements basic linked list. The code takes the number of elements in linked list as input from the user and also the elements. It stores them in linked list and then finally prints all the elements by traversing through all the elements. The code can be accessed from .java file present in the folder.
