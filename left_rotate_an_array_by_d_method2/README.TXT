This code uses JAVA, to acccess the code goto .java file. The main function takes the number of elements, the array elements and the number of times the left rotation to be performed from the user. The main function then calls LeftRotated() function which rotates the array d times in the left(counter-clockwise) direction. The main function then prints the updated array.
