This code takes circular linked list as input from the user and it also takes the element to be inserted at the end of the circular  linked list. The code inserts the new element at the end of the linked list and the code prints the modified linked list. To access the code goto .java file from the folder.
