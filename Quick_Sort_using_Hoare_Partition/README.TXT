This code takes array as input from the user and then sorts the array in ascending order using Quick Sort technique and the partitioning algorithm used here is Hoare partition algorithm. After sorting, the code prints the sorted array. The code for this can be accessed from .java file 
