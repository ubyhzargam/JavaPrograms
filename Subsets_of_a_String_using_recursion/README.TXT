This code uses recursion to find the total subsets of entered string which is input from the user using main function which then calls a recursive function to print all the subsets of that string which was passed by the main function to the recursive function. It also calculates and returns the total number of subsets possible to the main function which then prints it. The code can be accessed from .java file in the folder
