This code takes doubly linked list as input from the user. The code then reverses the linked list by modifying the next and prev addresses of individual nodes in the doubly linked list. The code finally prints the modified linked list. Goto .java file in the folder for the code.
